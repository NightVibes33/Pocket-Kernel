{
  "name": "pocketkernel-backend",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test test/*.test.js",
    "check": "node Scripts/validate_backend.mjs"
  }
}
