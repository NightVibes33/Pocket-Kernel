# Temporary Login Bypass

The account gateway is intentionally hidden until the production authentication services are configured.

Current launch flow:

1. Cinematic PocketKernel boot screen
2. Onboarding on first launch
3. PocketKernel command center

The Apple, Google, and passwordless email authentication implementation remains in the source tree and can be re-enabled without rebuilding the account system.
